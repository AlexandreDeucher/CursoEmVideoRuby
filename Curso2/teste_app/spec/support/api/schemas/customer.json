{
  "type": "object",
  "properties": {
    "customer" : {
      "required" : ["id", "description", "email"],
      "properties" : {
        "id" : {"type" : "integer"},
        "email" : {"type" : "string"},
        "description" : {"type" : "string"},
        "created_at" : {"type" : "string", "format": "date-time"},
        "updated_at" : {"type" : "string", "format": "date-time"}
      }
    }
  }
}